import { FeatureCard } from '../common/FeatureCard';import { ProductImage } from '../common/ProductImage';import { SectionShell } from '../common/SectionShell';import { landingContent as c } from '../../data/landingContent';import { productImages } from '../../data/productImages';
export function HeroSection(){return <SectionShell id="hero" className="hero"><div className="container hero-grid"><div><h1>{c.hero.title}</h1><p className="lead">{c.hero.subtitle}</p><p>{c.hero.description}</p><div className="grid-4">{c.hero.bullets.map((b)=><FeatureCard key={b} title={b} icon="◉" />)}</div></div><div><ProductImage src={productImages.heroMain} alt="hero"/><div className="grid-2"><ProductImage src={productImages.heroSupport} alt="support"/><ProductImage src={productImages.heroAlt} alt="alt"/></div></div></div></SectionShell>}
